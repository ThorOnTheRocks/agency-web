---
import Layout from '../../layouts/Layout.astro';
import { getPosts } from '../../services/content';
import { NewsletterSection } from '../../components/react/Newsletter';
import type { Post } from '../../services/content';

export const prerender = true;

const posts: Post[] = await getPosts();
---

<Layout
  title="Writing"
  description="Notes on product engineering, frontend systems, and delivery choices."
  fullBleed
>
  <section class="border-b border-border/70">
    <div class="site-container py-14 md:py-20">
      <header>
        <p class="eyebrow">Writing</p>
        <h1 class="mt-4 section-title max-w-2xl">Secondary notes on systems, architecture, and delivery.</h1>
        <p class="mt-5 max-w-2xl body-copy">
          Writing stays here as supporting context for how I think, not as the main commercial path through the site.
        </p>
      </header>
    </div>
  </section>

  <section class="site-container py-14 md:py-20">
    <div class="max-w-3xl divide-y divide-border/70 border-y border-border/70">
      {posts.length > 0 ? posts.map((post) => (
        <article class="group relative flex flex-col space-y-2 py-8">
          <div class="mb-1 flex items-center gap-4 text-xs uppercase tracking-[0.18em] text-muted-foreground">
            <time datetime={post.data.publishedAt?.toISOString() || ''}>
              {post.data.publishedAt ? new Date(post.data.publishedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'No date'}
            </time>
            <span class="h-3 w-px bg-border"></span>
            <span>Article</span>
          </div>

          <h2 class="text-2xl font-semibold tracking-tight transition-colors group-hover:text-primary">
            <a href={`/blog/${post.slug}`}>
              <span class="absolute inset-0"></span>
              {post.data.title}
            </a>
          </h2>

          <p class="text-muted-foreground leading-relaxed">
            {post.data.description}
          </p>

          <div class="pt-2 text-sm font-medium text-primary opacity-0 transition-opacity group-hover:opacity-100">
            Read Article &rarr;
          </div>
        </article>
      )) : (
        <div class="rounded-lg border border-dashed border-border/80 py-20 text-center">
          <p class="text-muted-foreground">No published notes yet.</p>
        </div>
      )}
    </div>
  </section>

  <div class="pt-2">
    <NewsletterSection client:visible />
  </div>
</Layout>
